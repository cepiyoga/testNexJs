/** @type {import('tailwindcss').Config} */
module.exports = {
    // In Tailwind CSS v4, the content option is not required.
    // If you later need to customize scanning behavior, consult the v4 docs.
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                primary: {},
                secondary: {},
                onduline: ''
            }
        },
    },
    plugins: [],
};
